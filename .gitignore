# Python bytecode — autogenerated, not source
__pycache__/
# Compiled bytecode — build artifacts
*.pyc

# Local virtualenv — machine-specific packages
venv/
# Env file (secrets) — may contain keys/passwords
.env/

# macOS Finder file — OS metadata
.DS_Store
# Windows thumbnail cache — OS cache file
Thumbs.db
